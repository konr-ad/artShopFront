services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artshop-frontend
    env_file:
      - .env
    ports:
      - "4200:80"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
